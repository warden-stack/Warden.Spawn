namespace Warden.Spawn.Security
{
    public class DefaultCredentialsConfigurator : ICredentialsConfigurator
    {
        public void SetConfiguration(string warden, object configuration, string watcher = "", string integration = "", string hook = "", string type = "")
        {
        }
    }
}